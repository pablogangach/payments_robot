# Python Project

This is a minimalistic Python project setup using Poetry for dependency management. The project follows best practices by separating source code and tests into different directories.

## Structure

- `src/`: Contains the source code.
- `tests/`: Contains the test code.

## Getting Started

To run the project, use the following command:

```bash
poetry run python src/main.py
```
